using UnityEngine;
using System.Collections;

public class s_fleches : MonoBehaviour {

	public GameObject fleche;
	public int fleche_h;
	public int fleche_b;
	public int fleche_g;
	public int fleche_d;

	private int orient;
	private int clock;
	private int clockEnd;
	// private int[] orientation = new int[] {1, 2, 3, 4};

	void Start () {
		clock =  (int)(Time.fixedTime) + 3;
	}
	

	void Update () {
		clockEnd = clock - (int)(Time.fixedTime);

	//	print (clock + " rr "+ clockEnd);

	//	if((s_pep_pressure.pressure < 0.2) && (clockEnd > 1000)) {
		//	orient = orientation[Random.Range(0, orientation.Length-1)];

			switch (Random.Range(1,5)) {
				/*	case 1:
				Instantiate (fleche, new Vector3 (Random.Range(3.5f, 4.5f), Random.Range(-3f, 3f), -1), Quaternion.Euler(0 , 90 , 270));
				fleche_h++;
				break;
			case 2:
				Instantiate (fleche, new Vector3 (Random.Range(3.5f, 4.5f), Random.Range(-3f, 3f), -1), Quaternion.Euler(0 , 270 , -270));
				fleche_b++;
				break;
			case 3:
				Instantiate (fleche, new Vector3 (Random.Range(3.5f, 4.5f), Random.Range(-3f, 3f), -1), Quaternion.Euler(270 , 0 , 0));
				fleche_g++;
				break;
			case 4:
				Instantiate (fleche, new Vector3 (Random.Range(3.5f, 4.5f), Random.Range(-3f, 3f), -1), Quaternion.Euler(90 , 180 , 0));
				fleche_d++;
				break;*/
			}
			clock = (int)(Time.fixedTime) + 3;
	//	} 
	}
}
